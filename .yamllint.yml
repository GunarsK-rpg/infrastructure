extends: default

rules:
  new-lines:
    type: platform
  line-length:
    max: 120
    level: warning
  truthy:
    allowed-values: ['true', 'false', 'on', 'off']
  comments:
    min-spaces-from-content: 1
  document-start: disable

ignore:
  - .git/
  - terraform/
